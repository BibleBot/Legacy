// rename to config.js
export default {
    // owner's ID
    owner: "",

    // API token
    token: "",

    // bot's username + discriminator
    botname: "",

    // name of the bot
    name: "",

    // instance identifier
    instance: "",

    // username + discriminator of various version adders (optional)
    versionAdders: [],

    debug: false
};
